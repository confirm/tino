---
services:


  # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  #
  # 🌍 Docs.
  #
  # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

  docs:

    #
    # 🔨 Build
    #

    build:
      context: .
      dockerfile: Dockerfiles/docs
      args:
        - MCI_DOCKER_REGISTRY

    image: ${MCI_DOCKER_REGISTRY}/typr/docs:${MCI_DOCKER_TAG}

    #
    # ⚡ Resources
    #

    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: '64M'

    #
    # 🔒 Security
    #

    security_opt:
      - no-new-privileges:true

    read_only: true

    tmpfs:
      - /tmp

    #
    # 🔄 Restart policy
    #

    restart: unless-stopped

  # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  #
  # 📝 Typr.
  #
  # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

  typr:

    build:
      context: .
      dockerfile: Dockerfiles/typr
      args:
        - MCI_DOCKER_REGISTRY

    image: ${MCI_DOCKER_REGISTRY}/typr/typr:${MCI_DOCKER_TAG}

